syntax = "proto3";

option go_package = "github.com/batchcorp/plumber/test-assets/shallow-envelope/shallow";

package shallow;

// Message is what goes into Envelope's Data field
message Payload {
  string name = 1;
}
